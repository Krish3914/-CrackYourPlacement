#include <bits/stdc++.h>
using namespace std;
static const int fast = []()
{
    std::ios::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);
    return 0;
}();
int main()
{
    int t;
    cin >> t;
    while (t > 0)
    {
        t--;
    }
    return 0;
}

// Press (ctrl + shift + B) to run on VS-code